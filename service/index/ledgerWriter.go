package index

import "github.com/onflow/flow-archive/ledgertmp"

// Registers writes the given registers in a batch to database
func (w *Writer) Registers(height uint64, registers []*ledgertmp.LeafNode) error {
}
